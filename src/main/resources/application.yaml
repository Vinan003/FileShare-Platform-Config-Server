spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MalikBandara/Fileshare-Platform-Project-Configurations.git
          search-paths: platform, services
          # Default branch eka 'main' nemei 'master' nam meka danna:
          # default-label: master 

server:
  port: 9000